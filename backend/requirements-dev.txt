-r requirements.txt

# Test dependencies. Pinned (not >=) because fastapi's TestClient depends
# on Starlette's test client, whose required companion package has changed
# names across versions (httpx -> httpx2) — an unpinned resolve can silently
# pull a Starlette version whose test-client dependency isn't installed,
# breaking `pytest` collection for every test that imports TestClient.
pytest==9.1.1  # CVE-2025-71176 / GHSA-6w46-j5rx-g56g fixed in 9.0.3+
httpx2==2.12.0
