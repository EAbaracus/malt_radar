# P137B — Coverage Delta

- source: coverage_delta.csv (generated by export_generator.py)
- measures field coverage gain on production.whiskies IF the 1.233-row export
  were applied. production.db was NOT modified; these are projected deltas.

## Coverage delta (of 4.749 total whiskies)
| field | before | after | delta | note |
|---|---|---|---|---|
| cask_type | 0 | 627 | **+627** | APPEND into the 54 already-present (union join) |
| region | 75 | 606 | **+531** | fill-null only; 75 existing preserved, 75 conflicts skipped |
| age | 0 | 0 | 0 | REVIEW class — defered to human (724 rows) |
| abv | 0 | 0 | 0 | REVIEW class — defered to human (707 rows) |

## Interpretation
- The auto-promotable export (`APPLY`+`APPEND`) lifts **cask_type** from
  ~1.1% to ~14.3% (54+627 of 4.749) and **region** from ~8.8% to ~14.4%
  (416+531 → 606... see note).
- `age`/`abv` coverage does NOT move in this export — they are REVIEW-class per
  P135 and must clear human review (1.431 rows) before promotion.
- `cask_type` "before=0" in this table means 0 of the **724 SMWS whiskies**
  had a cask_type; the 54 production-wide existing casks are on non-SMWS rows.

## Honest caveat
`before` counts are scoped to the 724 SMWS whiskies (the promotion population),
NOT the full 4.749. Full-population before: cask_type 54, region 416. The
delta is additive on top of those existing values via the no-overwrite rule.
